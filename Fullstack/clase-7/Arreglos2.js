const DiaClases= [true,false,true]
var Asistencias=0

for( var i=0; i < DiaClases.length;i++){ // var=i es Donde empieza el contandor; Si mi contador es menor al dia de [clases.los valores que tiene adentro, ve sumando 1+ a cada iteracion]
    if(DiaClases[i]){ //(siempre que sea true va a entrar) aqui se acceden a los elementos del arreglos
        Asistencias++; }
}
console.log (Asistencia)
